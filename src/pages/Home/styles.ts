import styled from 'styled-components'

export const HomeContent = styled.main`
  --padding-horizontal: 2rem;
  max-width: calc(864px + var(--padding-horizontal) * 2);
  margin: 0 auto;
  padding: 0 var(--padding-horizontal) 4rem;
`
